{
  "name": "yks-kocluk",
  "private": true,
  "description": "YKS koçluk web — Appwrite şema scriptleri için bağımlılıklar",
  "scripts": {
    "setup:appwrite": "node setup-appwrite.js",
    "setup:deneme": "node setup-appwrite.js --only-deneme",
    "setup:exam-results": "node setup-exam-results.js",
    "setup:seed": "node setup-appwrite.js --seed",
    "upload:avatars": "node scripts/upload-avatar-pack.cjs",
    "appwrite:setup": "node appwrite-setup.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "node-appwrite": "^22.1.3"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
